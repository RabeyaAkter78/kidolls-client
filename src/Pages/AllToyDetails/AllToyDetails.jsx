import { useLoaderData } from "react-router-dom";

const AllToyDetails = () => {
    const data = useLoaderData()
    console.log(data)

    return (
        <div>

        </div>
    );
};

export default AllToyDetails;